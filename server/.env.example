# Paste the connection string from MongoDB Atlas here. Do not commit server/.env.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/stack-tower?retryWrites=true&w=majority

# Optional server configuration
PORT=5001
CLIENT_URL=http://localhost:5173
